import React from 'react';
import MapForm from '../components/Map/MapForm';

const {kakao} = window;

const Map = () => {
   return (
      <div>
         <MapForm/>
      </div>
   );
};

export default Map;